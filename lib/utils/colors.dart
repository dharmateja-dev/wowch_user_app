import 'package:booking_system_flutter/utils/configs.dart';
import 'package:booking_system_flutter/utils/theme_colors.dart';

// Primary Colors - Using theme colors for consistency
var primaryColor = defaultPrimaryColor;
const secondaryPrimaryColor = LightThemeColors.secondaryPrimary;
const lightPrimaryColor = LightThemeColors.lightPrimary;
const primaryLightColor = LightThemeColors.primaryLight;

//Text Color - Using theme colors
const appTextPrimaryColor = LightThemeColors.textPrimary;
const appTextSecondaryColor = LightThemeColors.textSecondary;
const cardColor = LightThemeColors.cardBackground;
const borderColor = LightThemeColors.border;

// Dark Theme Colors - Using theme colors
const scaffoldColorDark = DarkThemeColors.scaffoldBackground;
const scaffoldSecondaryDark = DarkThemeColors.scaffoldSecondary;
const appButtonColorDark = DarkThemeColors.appButtonBackground;

// UI Colors - Using AppColors for consistency
const ratingBarColor = AppColors.ratingBar;
const verifyAcColor = AppColors.verifyAc;
const favouriteColor = AppColors.favourite;
const unFavouriteColor = AppColors.unFavourite;
const lineTextColor = AppColors.lineText;

//Status Color - Using AppColors
const pending = AppColors.pending;
const accept = AppColors.accept;
const on_going = AppColors.onGoing;
const in_progress = AppColors.inProgress;
const hold = AppColors.hold;
const cancelled = AppColors.cancelled;
const rejected = AppColors.rejected;
const failed = AppColors.failed;
const completed = AppColors.completed;
const defaultStatus = AppColors.defaultStatus;
const pendingApprovalColor = AppColors.pendingApproval;
const waiting = AppColors.waiting;

// Activity Colors - Using AppColors
const add_booking = AppColors.addBooking;
const assigned_booking = AppColors.assignedBooking;
const transfer_booking = AppColors.transferBooking;
const update_booking_status = AppColors.updateBookingStatus;
const cancel_booking = AppColors.cancelBooking;
const payment_message_status = AppColors.paymentMessageStatus;
const defaultActivityStatus = AppColors.defaultActivityStatus;

// Other Colors - Using AppColors
const walletCardColor = AppColors.walletCard;
const showRedForZeroRatingColor = AppColors.showRedForZeroRating;

//Dashboard 3 - Using AppColors
const jobRequestComponentColor = AppColors.jobRequestComponent;
const dashboard3CardColor = AppColors.dashboard3Card;
const cancellationsBgColor = AppColors.cancellationsBg;

// Common Colors - Using AppColors
const greyColor = AppColors.grey;
const grey300Color = AppColors.grey300;
const amberColor = AppColors.amber;

const shopColor = AppColors.shop;

// Dark Theme Divider Color
const dividerDarkColor = DarkThemeColors.divider;
